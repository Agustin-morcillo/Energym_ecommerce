<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head") %> 
<body>
    <%- include("../partials/header") %> 

    <main>
        <section>
            <article>
                <div class="registration-main-container">
                    <div class="registration-form-container">
                        <div class="sections-welcome-titles-container">
                            <h2 class="sections-welcome-titles">Datos de la cuenta</h2>
                            <p class="sections-welcome-description">Completa los datos o registrate con Google o Facebook</p>
                        </div>

                        <form action="/users/register" class="form" method="POST" enctype="multipart/form-data">
                            <div class="register-info-container">
                                <label for="name-register" class="register-label">Nombre<span class="required-field">*</span></label>
                                <input name="name" type="text" id="name-register" class="register-input inputs-fields">
                                <p class="validation-error-register">
                                    <%= typeof errors == "undefined" ? "" : typeof errors.name != "undefined" ? errors.name.msg : "" %>
                                </p>
                            </div>

                            <div class="register-info-container">
                                <label for="lastname-register" class="register-label">Apellido<span class="required-field">*</span></label>
                                <input name= "lastName" type="text" id="lastname-register" class="register-input inputs-fields">
                                <p class="validation-error-register">
                                    <%= typeof errors == "undefined" ? "" : typeof errors.lastName != "undefined" ? errors.lastName.msg : "" %>
                                </p>
                            </div>

                            <div class="register-info-container">
                                <label for="email-register" class="register-label">Email<span class="required-field">*</span></label>
                                <input name= 'email' type="text" id="email-register" class="register-input inputs-fields">
                                <p class="validation-error-register">
                                    <%= typeof errors == "undefined" ? "" : typeof errors.email != "undefined" ? errors.email.msg : "" %>
                                </p>
                            </div>

                            <div class="register-info-container">
                                <label for="retype-email-register" class="register-label">Repita su email<span class="required-field">*</span></label>
                                <input name= 'retypeEmail' type="text" id="retype-email-register" class="register-input inputs-fields">
                            </div>
                            
                            <div class="register-info-container">
                                <label for="password-register" class="register-label">Contraseña<span class="required-field">*</span></label>
                                <input name= 'password' type="password" id="password-register" class="register-input inputs-fields">
                                <p class="validation-error-register">
                                    <%= typeof errors == "undefined" ? "" : typeof errors.password != "undefined" ? errors.password.msg : "" %>
                                </p>
                            </div>

                            <div class="register-info-container">
                                <label for="repassword-register" class="register-label">Repita su contraseña<span class="required-field">*</span></label>
                                <input name= 'retype' type="password" id="repassword-register" class="register-input inputs-fields">
                            </div>

                            <div class="register-info-container">
                                <label for="avatar-register" class="register-label avatar-label">Avatar</label>
                                <input type="file" id="avatar-register" name="avatar" class="register-avatar-input" accept="image/jpg, image/png, image/jpeg">
                            </div>
                            <div class="register-img">
                                <p class="validation-error-register">
                                    <%= typeof errors == "undefined" ? "" : typeof errors.avatar != "undefined" ? errors.avatar.msg : "" %>
                                </p>
                            </div>

                            <div class="button-container register-info-container position-class">
                                <button type="submit" class="register-button final-register-button buttons-style" >Finalizar registro</button>
                            </div>
                        </form> 

                        <form action="/" method="GET" class="facebook-google-form">
                            <div class="button-container button-info-container">
                                <button type="submit" class="register-button google-button buttons-style"><i class="fab fa-google google-icon"></i>Registrarse con Google</button>
                            </div>
                        </form>


                        <form action="/" method="GET" class="facebook-google-form">
                            <div class="button-container button-info-container">
                                <button type="submit" class="register-button facebook-button buttons-style"><i class="fab fa-facebook facebook-icon"></i>Registrarse con Facebook</button>
                            </div>
                        </form>
                    </div>
                </div>
            </article>
        </section>
    </main>

    <%- include("../partials/footer") %> 
    
</body>
</html>